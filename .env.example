# Copy this file to .env and fill in real values. Never commit .env.

# Groq API key (OpenAI-compatible). Get one free at https://console.groq.com
LLM_API_KEY=

# Travelpayouts (Aviasales) Data API token — free affiliate signup at
# https://www.travelpayouts.com  (used for cached flight prices)
TRAVELPAYOUTS_TOKEN=
